import pandas as pd
import numpy as np

def my_model(p):
    '''
    '''
    system_observation = pd.DataFrame(np.random.randn(10, 5),columns=['a', 'b', 'c', 'd', 'e'])
    return system_observation,
